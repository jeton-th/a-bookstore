import React from 'react';

const LoadingSpinner = () => (
  <div style={{ textAlign: 'center' }}>
    <img src="../assets/spinner/spinner.gif" alt="spinner" />
  </div>
);

export default LoadingSpinner;
